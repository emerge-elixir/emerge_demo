Blocked-path demo image source note:

- `blocked.jpg`: duplicate of `../demo_images/runtime.jpg`

The example app keeps this file at a separate path so the blocked runtime-path
demo can point at a real image that sits outside the configured allowlist.

Original upstream source and context are recorded in `../demo_images/SOURCES.md`.
This file is repo-only and is not included in the Hex package.
